import 'package:flutter/material.dart';
import 'package:sps_ble/COMMON_ASSETS/common.dart';

class CustomMode extends StatelessWidget {
  const CustomMode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: CommonAppbar(
         
        ),
      ),
      body: Column(),
    );
  }
}

